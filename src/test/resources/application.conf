ktor {
    application {
        modules = [ org.kryptokrona.api.ApplicationKt.module ]
    }
}

db {
    url = "jdbc:postgresql://127.0.0.1:5433/kryptokrona_api_test"
    url = ${?DB_URL}
    user = postgres
    user = ${?DB_USER}
    password = test1234
    password = ${?DB_PASSWORD}
    driver = "org.postgresql.Driver"
    driver = ${?DB_DRIVER}
}